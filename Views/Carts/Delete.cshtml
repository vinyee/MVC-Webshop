@model DemoWithUsers.Models.Carts

@{
    ViewBag.Title = "Delete";
}

<h3>Är du säker på att du vill appen ta bort från din köplista?</h3>
<div>
    <hr />

    @using (Html.BeginForm())
    {
        @Html.AntiForgeryToken()

        <div class="form-actions no-color">
            <a href="@Url.Action("Index", "Carts")" class="btn btn-default">
                Tillbaka
            </a>
            <input type="submit" value="Ta bort" class="btn btn-danger" />
        </div>
    }
</div>
